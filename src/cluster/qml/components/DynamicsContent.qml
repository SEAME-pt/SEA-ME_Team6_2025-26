import QtQuick
import ClusterTheme 1.0

Item {
    anchors.fill: parent
    Text {
        anchors.centerIn: parent
        text: "To be added..."
        color: BaseTheme.white
        font.pixelSize: 18
    }
}